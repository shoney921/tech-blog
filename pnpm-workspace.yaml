# esbuild 빌드 스크립트 승인 설정.
# pnpm 11부터 package.json의 "pnpm" 필드를 읽지 않고 키 이름도 바뀌었다.
# Cloudflare Pages 빌드 커맨드가 npx pnpm@latest 를 쓰므로 두 형식을 모두 둔다.
#
# pnpm 11+ : allowBuilds (map 형식)
allowBuilds:
  esbuild: true
# pnpm 10  : onlyBuiltDependencies (list 형식)
onlyBuiltDependencies:
  - esbuild
